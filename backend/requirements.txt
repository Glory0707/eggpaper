# eggpaper 后端依赖（Python 3.10+）
pymupdf>=1.24
pillow>=10.0
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pyyaml>=6.0
python-multipart>=0.0.9

# 扫描件 OCR（导入时自动识别无文字层的 PDF）：RapidOCR = PP-OCR 中英混合模型 + onnxruntime
# numpy 是 onnxruntime 的硬依赖——eggpaper.spec 的 excludes 里因此不能瘦身 numpy
rapidocr-onnxruntime>=1.3.24

# 整本双语翻译引擎（可选，subprocess 隔离调用，AGPL 与本项目同生态）
# pdf2zh>=1.9
# 注意：需配合 tencentcloud-sdk-python-tmt==3.0.1300（新版 SDK 缺少 pdf2zh 依赖的类）

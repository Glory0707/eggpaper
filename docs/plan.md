# eggpaper 实施计划

> 配合 design.md v0.2。每个任务带验收标准；每个里程碑结束做一次功能走查。

---

## M2 · 翻译三模式 + 界面减法 + 一致性补漏 ✅ 已完成（2026-09-11，走查通过）

### T1 翻译三模式

| # | 任务 | 验收 |
|---|---|---|
| T1.1 | 后端 variant=mono（mono.pdf 已在 data/translated，补路由与状态判断） | 「译文」模式加载 mono.pdf；未生成时按钮置灰 |
| T1.2 | 顶栏 segmented 扩为 原文 / 译文 / 双语 | 三态切换正确，切换后保留当前缩放与页码附近位置 |
| T1.3 | 双语对开视图：左原文页 + 右译文页成对滚动（dual 偶数页配对），双文档渲染 | 9 页论文渲染 9 个 spread；左右页码对应；无渲染饿死 |
| T1.4 | 双语子选项「对开 / 交替」，默认对开 | 两模式切换即生效；选择记忆到 localStorage |
| T1.5 | 角色/眉批图层在三模式下行为定义：原文与对开的原文页显示批注；译文页与整版译文不显示 | 各模式截图确认 |

### T2 界面减法（逐项过，删完复查）

| # | 删除/修改项 | 验收 |
|---|---|---|
| T2.1 | 删页面底部 colophon 免责行；"推断"提示移入角色卡内部一行小字 | 页面无固定文案残留 |
| T2.2 | 删左栏底部「文献·术语·批注 全部留在本机」 | 同上（该信息只留在设置弹窗） |
| T2.3 | 顶栏 VOL.0.1 · 阅读过默 → 仅 v0.1 | 视觉噪点减少 |
| T2.4 | 文档元信息去掉文件名（标题已展示） | 「9 页 · 23 段」足够 |
| T2.5 | 角色图例只显示计数>0 的角色 | 图例更短 |
| T2.6 | 全 UI 文案通读一遍：重复出现的信息只保留一处 | 走查通过 |

### T3 术语一致性补漏

| # | 任务 | 验收 |
|---|---|---|
| T3.1 | 问答/总结 system prompt 注入命中术语表 | 问"这篇的 trade-off 结论"回答使用锁定译法 |
| T3.2 | 术语表导出 CSV → pdf2zh `--glossary-files` | 整本双语中该术语译法与术语表一致 |

### T4 功能走查（每功能实测顺手度）

走查协议：每项功能按脚本操作，记录 完成秒数 / 卡点 / 情绪；卡点修复或砍功能，不修说明理由。

| 功能 | 任务脚本 | 通过线 |
|---|---|---|
| 导入 | 从 Zotero 拖一篇 PDF 进窗口 → 自动解析 | ≤20s 看到可读页面 |
| 析读 | 点析读 → 等待 → 骨架出现 | 等待有进度反馈，结果一眼能懂 |
| 精读 | 开略读扫核心段 → 关略读 → hover 任一标签 → 改判一个角色 | 改判立即生效并持久 |
| 眉批 | 生成眉批 → 点一条跳原文 → 看高亮定位 | 定位准确率抽查 ≥90% |
| 翻译 | 三模式各切一遍；划词翻译一个术语；收进术语表 | 全程无说明文字也能完成 |
| 问答 | 问固定 3 问 → 点引用跳转 | 引用跳转准确 |
| 删除 | 删一篇 → 文库与视图正确清空 | 无残留 |

**M2 出口验收**：三模式可用 + UI 无冗余文案 + 走查清单全过 + 截图存档 `_qa/shots/`。

---

## M2.5 · 阅读工效 ✅ 已完成（2026-09-11，走查 5 项 PASS）

| # | 任务 | 验收 |
|---|---|---|
| T5.1 | 位置记忆：重开论文恢复滚动位置+模式+图层 | 关掉重开回到原位 |
| T5.2 | 跳转栈：所有跳转入栈，Alt+← 返回 | 引用跳转后能一键回原位 |
| T5.3 | 键盘流：j/k/t/s/f/1/2/3///x/g l/Alt+←/?,? 卡可自发现 | 全程不碰鼠标完成一次精读循环 |
| T5.4 | 查译钉页边：划词气泡加「钉在页边」，查译卡与眉批同列、标"你" | 关气泡后查译仍在，导出可见 |
| T5.5 | 段落把手：hover 段落页边浮出「译此段/问这段」，段译钉页边 | 不碰右栏完成段级翻译与提问 |
| T5.6 | 左栏收纳 44px 图标条（浮出面板）+ 右栏可折叠 + 双语对开自动收右栏/隐藏页边 | 阅读态正文宽度显著增加；三态姿势自动切换 |
| T5.7 | 角色改判"已改"角标 + 回到推断 | 撤销路径可用 |

**M2.5 出口验收**：走查协议重跑一遍（导入/析读/精读/眉批/翻译/问答/删除），全部通过线不变；另加"全程键盘精读循环"通过。

---

## M2.6 · 视觉与动效打磨 ✅ 已完成（2026-09-11）

## M2.7 · 视觉二轮 + AI 主动 ✅ 已完成（2026-09-11）

- 字体系统二轮：内容/界面切换 Inter + JetBrains Mono（衬线退位为品牌声，仅 wordmark）——解决"僵硬"反馈
- Logo 重设计：剥壳蛋（蛋体+裂纹+蛋黄，大尺寸加揭盖），新增 favicon
- AI 主动：导入即后台通读（打开即简报）、论文专属推荐问题（析读后自动生成 4 问，缓存）
- 页边批注卡高估算修复（消除堆叠重叠）
- 注：T3.2 pdf2zh glossary 参数被经典版引擎阻断（1.9.x 无此参数），记录于 design.md 已知限制

| # | 任务 | 验收 |
|---|---|---|
| T6.1 | 字体四声系统落地：serif 内容 / sans 控制 / mono 标签 / kai 人声，字号阶梯收敛到 7 档 | 一屏内家族 ≤3、字号 ≤4 |
| T6.2 | 配色 token 化：纸面四层亮度分层、墨色三级、阴影=悬浮纪律（静态布局去阴影） | 目检 + 截图对比 |
| T6.3 | 动效 token 化：三缓动三时长；气泡/折叠/模式切换/位置恢复按清单执行 | 所有过渡 ≤320ms |
| T6.4 | 点睛三件：略读 stagger 波浪、眉批引文描画+页边引线、析读笔画进度线 | 录屏 60fps 目检 |
| T6.5 | `prefers-reduced-motion` 全局支持 | 系统开启后零动画 |
| T6.6 | 微巧思：拖入蛋壳裂纹、析读完成蛋黄一颤（各仅一个触发态） | 不影响主流程 |

**M2.6 出口验收**：visual.md §5 五条全过 + 走查协议重跑。

---

## M3 · 沉淀与效率（部分完成 2026-09-11）

✅ 1 方法卡 · 2 本文缩写表 · 4 Markdown 导出 · 5 术语导出 CSV · 6 骨架 mini-map
✅ 3 图表速览画廊 + 灯箱/跳原文（2026-09-11 第四轮）
⬜ 7 图表放大镜（矢量图区域检测增强，下一批）

1. 方法卡片（protocol 卡）：验收=一篇实验论文抽出可复现的材料/参数/步骤三段
2. 本文缩写表：骨架时顺带抽取；进术语 tab；可一键入术语表
3. 图表速览画廊：Figure 连图注；验收=图页论文不用读正文即可讲出主线
4. Markdown 导出：骨架+眉批+查译+一眼卡 → 单文件 .md；验收=导入 Obsidian 不乱
5. 术语导出 CSV/Anki
6. 骨架 mini-map：滚动跟随高亮当前节，"读到哪了"可见（scroll-spy）
7. 图表放大镜：hover 图区放大 + 「问这张图」（与图表速览合流，依赖图形区域检测）

## M4 · 进阶（按需启动）

框选区域视觉问答（需配置视觉模型）· 数据对比表 · 批量速览 · 审稿模式 · Zotero 深度集成 · Tauri 打包 · OCR

---

## 技术债与备忘

- LLM 输出归一化已做（键/枚举/范围校验），新功能沿用，禁止静默吞错
- 推理型模型（glm-5.3-flash）max_tokens 要留思考额度：骨架 16k、眉批 12k，新调用照此量级
- pdf2zh：google 在国内网络挂起（无快速失败），默认服务已是 bing；后续可给 subprocess 加首输出超时探测
- 骨架/眉批 prompt 的角色体系按化学定制；扩展学科时做成可配置模板而非硬编码
- 解析器对单栏/其他排版未系统验证，遇到再说（修复优先于预防）

## M3.2 · 视觉三轮 ✅ 已完成（2026-09-11）

- 骨架 tab 去盒子化：结构条（角色构成一瞥）+ 排版化论证链（层级靠字重，不靠边框）
- 骨架 mini-map：滚动跟随"读至 ¶n"+ 当前主张高亮
- 论证漫游：▶ 沿证据链 2.6s/站自动巡航，Esc/点按停止
- 图表速览：栅格图区域抽取 + 画廊 + 灯箱 + 跳原文

## M3.4 · 体感修整（用户五条反馈）✅ 已完成（2026-09-11）

用户原话逐条落地，验收全部走浏览器实测（1600×1000 与 1280×760 两档窗口）：

| # | 反馈 | 处理 | 实测证据 |
|---|---|---|---|
| 1 | logo 还是像水滴，因为上面是尖的 | 蛋形重画：上下两个半椭圆在最宽处相切（上半径 40 < 下半径 52），钝端朝上、尖端朝下；裂缝改斜向从左腰爬到右上壳沿 | 16/25/60/116/190/340px 六档目检，蛋形稳定可辨；favicon 用简版（16px 下裂缝糊成一团） |
| 2 | 字体太杂乱，别用楷体，要饱满、要统一 | 四声字体砍成两声 + 一个署名：删掉 `--kai`（楷体在 Windows 太细太干瘪），全部人声文本归 Inter/雅黑；Fraunces 只留 wordmark | CDP `CSS.getPlatformFontsForNode` 验证：中文全部 Microsoft YaHei UI，拉丁 Inter，无楷体残留 |
| 3 | 旁批的 1/2/3 不知何用、点了没反应、会闪退、长批注展开压住下一条 | 书签改成**角色首字**（背/缺/主/证/对/流/拓/限）；角色卡改为 fixed 定位、**只由点击开关**（原 hover 卡与被 hover 的标签之间有 10px 死区，鼠标一离开就卸载，所以永远点不到 = "闪退"）；卡上写明出处与改判入口 | 角色卡四条开关路径全过：Esc ✓ 点外 ✓ 同书签再点 ✓ 悬停不弹 ✓ |
| 4 | 长批注展开覆盖下一条 | 高度不再估算，改为**渲染后实测再摆第二遍**；展开时下方批注整体下让，页边随之变长；正文卡内滚动 ≤240px | 逐页把所有可展开批注展开：overlap=0 spill=0，页边 818→974 长出来消化掉 |
| 5 | 右侧分工不明，本文缩写该进术语 | 本文缩写从「速览」移到「术语」并圈进 200px 滚动区（21 条缩写不再把术语表顶到屏幕外）；骨架页签补角色图例（首字+全称+条数，点击跳该角色首段） | 术语表表单 y=360，无需滚动即可见 |
| 6 | 框选后想取消找不到按钮 | 框选模式加**顶部常驻提示条**（框选 / 说明 / Esc / 退出按钮）；选区气泡的 × **常显**（原先只在出答案后才出现）；Esc 一键退出；点浮层外关闭 | hint=1 exitBtn=1；visClose 常显 ✓；Esc 后 frame 按钮熄灭 ✓ |

顺带修掉三处旧疾：
- 页边批注原来只有「查译」能给删，自动眉批删不掉 → 所有批注都可 ×，后端本来就支持
- 框选答疑钉页边时写死 `para_idx:0,page:0`，永远落在第一页第一段 → 改为记录框选所在的页/段/PDF 坐标矩形；同段落多个区域答疑不再互相覆盖（`/pin` 带 rect 时不参与按段去重）
- 三态/对开/缩放的**选择只在滚动时才存**，刷新后经常回到上一个模式 → 换姿势即存

浮层定位统一走书桌中线（`.desk-float` 用 `translate` 而非 `transform`，这样 `pop` 动画不会把居中偏移冲掉）；缩放条、返回原位、框选提示条三件浮层现在都正对书桌，不再偏到右栏底下。

### M3.4 补遗 · 「问/译」把手点不到（同日第二起）

用户报告：鼠标放到论文上会出现「问」「译」两个小字，移过去想点就消失。根因有三处叠加：

1. **2px 死区**：把手定位在纸缘外 2px，鼠标从正文出来穿过缝隙瞬间触发 `.page` 的 mouseleave，把手当场卸载——永远追不上。修复：把手加 16px 透明桥面搭在纸边上（`left: calc(100% - 16px); padding-left: 16px`），桥面是 `.page` 的后代区域，指针走桥面不触发 mouseleave。
2. **把手钉在段顶**：鼠标常悬在段落中部，按钮却在段落第一行的位置，斜着走很容易出界。修复：把手纵位跟鼠标（夹在段落带内），向右一小步就够到；另加 350ms 消失宽限，斜穿缝隙也不再半路蒸发。
3. **每页都长一个把手**：`v-if` 只判了 `hoverPara != null && it.text`，没判段落属不属于这页——悬停任何一段时 9 页页角各冒出一组野把手。修复：补 `paraByIdx[hoverPara]?.page === it.origPage`。

实测：悬停 → 水平直移（-14/-4/中心三点采样）→ 停留 400ms → 点击「问」，全程把手存在，右栏切到提问页并预填「¶1 这段在说什么？」；「译」走 mock 验证钉页边成功。测试中被 mock 覆盖的真实查译笔记（¶3）已用真实 API 重译恢复。

顺手根治了切换模式时偶发空白页：pdf.js 不允许并发 render 同一 canvas，快速连切三态时两个渲染任务会撞车。改为按画布把渲染任务串成队列链，轮到执行时重新对齐当前 scale/DOM，过期的排队任务直接作废。10 次连切回归：9/9 页渲染、零报错。

### M3.5 · 配色提亮 + 去把手 + 字号收敛（用户三条反馈）✅ 已完成（2026-09-11）

| # | 反馈原话 | 处理 |
|---|---|---|
| 1 | 配色不好看，底色有点昏暗 | 纸色从暖黄 #EFE8D8 改为中性亮灰 #F4F2ED，内容纯白；**删掉那层噪点颗粒**（意图是"台灯下的纸"，实测就是一层灰膜，文字也不够锐）；新增 --card 面板色 #FBFAF7，形成 桌面/沟槽/面板/内容 四级亮度 |
| 2 | "问""译"这两个功能不需要，勾选了本来也可以问译 | 段落 hover 把手整个删除：模板、样式、`hoverPara`/`handleY`/`handleHold`/`onPageMove`/`askPara` 全清。段译入口保留键盘 `t`（译当前阅读段并钉页边），划词气泡本来就是完整的（翻译/钉页边/收进术语/提问） |
| 3 | 字体类型和大小还是很繁杂 | ①**字号 17 种 → 4 档 + display**（15/13/12/10.5 + wordmark 21），styles.css 里 79 处硬编码字号与模板里全部内联字号统一走 token；②**等宽字体只留编号位**：小节标签原先"等宽+大写+0.13em 字距"，一屏几十个小灰字全在抢注意力，现改回正常字体（600 字重 + 0.04em）；右栏页签、略读/框选开关、一眼卡的"贡献/方法/发现"、问答的"你/EGGPAPER"一并换掉 |

顺带：右栏**结构条删除**——它与新加的角色图例说的是同一件事（构成比例），两张图叠着就是冗杂；图例本身也去盒子（原来 8 个带边框的 chip，改成方块+文字+条数，靠留白分块，跟全站"静态布局不用装饰性边框"的纪律对齐）。图例文案改为"段落角色 · 点一下跳到该类首段"，兼作页边书签的说明书。

实测（浏览器采样全 DOM，剔除论文自身的 textLayer）：界面字号只剩 21/15/13/12/10.5 五种，字体家族 Inter（正文）+ JetBrains Mono（28 处，全是 ¶号/页码/kbd/百分比）+ Fraunces（1 处，wordmark）；`.para-handle` 计数 0；划词气泡五个动作完好；角色图例点击能跳；控制台零报错。

#### M3.5 补遗 · 改判回退与划词锚点（同日第三、四起）

排查上面那轮时顺手撞出来的两个真 bug：

1. **「回到推断」是坏的**。角色卡的下拉有「回到推断」（值 `""`），但后端 `override-role` 用 `role not in llm.ROLES` 校验，空串直接被判非法返回 400；前端又是内联 `await`，异常被静默吞掉——用户点了没反应，也不知道为什么。修复：后端空串 = 回到推断（`db.override_annotation` 本来就实现了这条分支，只是永远走不到）；前端把 `@override` 提成具名 handler，失败 toast 报出来。实测：改判 → 出现「已改判」小旗 + toast；回到推断 → 小旗消失、角色还原、toast「已回到推断」；非法角色仍被拒。
2. **划词定位不到段落时会把笔记钉到第一页**。`onMouseUp` 里 `paraIdx`/`page` 默认值是 `0`，段落命中失败就原样发出去，笔记于是落在 ¶0（第一页页边）上，而内容是别页的文字。修复：页码用选中文字所在页兜底，`para_idx` 记 −1（−1 是有效 int，能穿过 `int(body.get(...) or 0)`，且不会和任何真段落撞去重）。实测在第 3 页划词，payload 是 `{para: 9, page: 2}`。

另外把角色卡从「一滚就消失」改成**跟着书签走**：卡片位置每帧按书签的新矩形重算（rAF 节流），书签滚出书桌视野才自动收起。之前只要点击时浏览器为了把书签滚进视野而滚一下，滚事件就会立刻把卡片关掉（自动化里 100% 复现，人手速慢时也会遇到）。

### M3.6 · 交互手感（用户：要 macOS 那种灵动、流畅、不花哨）✅ 已完成（2026-09-11）

不是"多加点动画"，是先找出四类**跳变**，再逐条消掉。

| # | 问题 | 处理 |
|---|---|---|
| 1 | 所有浮层的**退场都是硬切**（只有入场动画：文库面板、设置弹窗、划词/框选气泡、角色卡、灯箱、快捷键卡、toast 全是 `v-if` 直接消失） | 全部改 `<Transition>`，统一 `pop`/`fade`/`slide-l` 三套；退场 120ms 短于入场 200ms |
| 2 | 换原文/译文/双语时**先闪一个"正在摆上书桌"加载器**，而且**读完的位置丢了**（切一下跳回第 1 页） | 换模式不再走加载器，改书桌顶部一条 2px 细进度线（不遮内容）；位置按「原文页码 + 页内比例」锚点落回 |
| 3 | 三个模式的**页号不是一回事**（原文 9 页 / 译文 9 页 / 双语 18 页，2i=原文 2i+1=译文），跳转和位置记忆会错位；更糟的是译文模式每页 `origPage=-1`，**`j/k` 步进、骨架跳转在译文模式下静默失效** | 加 `origPageOf()`/`pageItem()` 统一换算；`paraAbsY`/`applyJump` 都走它，译文模式下步进与跳转恢复 |
| 4 | 折叠右栏 / 缩放导致重排时**视线跳走** | 重排前记锚点、重排后落回；锚点只在一次 resize 连发的第一拍记录（右栏是 320ms 动画，中途每帧都重记会记到"新宽度+旧页高"的错位坐标） |

另外两处"花哨"：按钮 hover 的**悬浮上浮**（`translateY(-1px)` + 阴影）去掉，改成背景换色 + 按下压深；分段控件（原文/译文/双语）从"选中块瞬间变色"改成 **macOS 式滑块**——选中块自己滑过去，三个按钮强制等宽、滑块按 1/n 对齐（实测位移 0 / 47.99 / 95.98px，按钮均 48px）。缩略图 hover 从 3% 收到 2%。旁批被推开时改成 `transition: top 260ms` 滑过去而不是瞬移。

落位为什么要"量一次核对、偏了就再落一次"：它要跟换模式渲染、右栏宽度动画、画布重定标一堆异步赛跑，赌一次成功赌不赢。实测连续切 8 次模式，页码稳定停在原文第 1 页、页内比例 0.87→0.94（累计漂移 <0.07）。用户一旦自己滚动立刻停止追位。

**一条设计纪律（写进 visual.md）**：入场用 keyframes 而不是 transition——transition 的起点类要靠 `requestAnimationFrame` 摘掉，rAF 被节流时元素会卡在全透明；**内容绝不能挂在一个可能跑不动的过渡上**。这条是本次实测环境里发现的（本机 headless 浏览器动画时钟停摆，`getAnimations()[0].currentTime` 恒为 0），顺手把这条风险从设计里摘掉了。

### M3.7 · 色标重做：八色相 → 四档注意力（用户：彩色的图表太花哨）✅ 已完成（2026-09-11）

用户指着右栏"段落角色"那张图说太花哨。根因是**我自己违反了写进 visual.md 的纪律**——那条写着"八角色色只是调味，只以 ≤14px 书签片、3px 竖条出现，**永不作填充色**"，但角色书签和后来的图例色块恰恰是八个饱和色块平铺。

更根本的问题：**人能一眼解码的颜色上限是 3–4 个**。八个色相记不住，于是"对照参比 vs 优化拓展"这种细类其实没人能扫一眼认出来——颜色在制造噪音，不在传递信息。而"这段是全文的芯，还是铺垫"才是扫页边时真正要判断的。

改法：**色标只表达注意力**。一个暖色 + 三级灰，从深到浅就是"论证主干 → 外围让步 → 铺垫流程"：

| 档 | 填充 | 角色 |
|---|---|---|
| 核心主张 | `#D08A1C` 琥珀 | 全文的芯 |
| 论证主干 | `#57534A` 深灰 | 关键证据 · 缺口转折 |
| 外围与让步 | `#8E8A80` 中灰 | 对照参比 · 优化拓展 · 让步局限 |
| 铺垫与流程 | `#C9C4BA` 浅灰 | 背景铺垫 · 标准流程 |

眉批（原本九个色相）同样收敛成四档语义：值得读 / 要当心 / 是噪音 / 你自己钉的。

顺带：
- **图例即样本**——右栏那张表就是页边色块的样本（同色块、同字），两列素排、按注意力从深到浅排序（排序本身也是信息），去掉边框（边框会让 8 项看起来像 8 个按钮）。
- **文字色与填充色分家**：浅灰当文字在白底上看不见，所以另给一档正文可读的文字色阶梯（`ROLE_TEXT_COLOR` / `KIND_TEXT_COLOR`）。
- **对比度逐个核过**：四档色块字色 5.7 / 7.7 / 4.5 / 9.0，四档正文色 5.2 / 17.2 / 7.8 / 5.3，眉批标签 5.2 / 6.9 / 5.3 / 12.3——全部过 WCAG AA 4.5:1（原先八色相里有一半不达标）。

### M3.8 · 删掉解释设计的文案（用户：继续删无用的说明）✅ 已完成（2026-09-11）

用户点着右栏那句"段落角色 · 色越深越靠近论证主干"说，这类说明该继续删。通读一遍，界面上确实积了一批**在解释设计而不是在交待功能**的字，逐条清掉：

| 位置 | 原文 | 现在 |
|---|---|---|
| 右栏图例标题 | 段落角色 · 色越深越靠近论证主干 | 段落角色 |
| 右栏图例下方 | 23 段 | 删（顶栏已有"9 页 · 23 段"） |
| 右栏分节 | GAP · 作者的出发点 | GAP |
| 右栏分节 | CLAIMS → EVIDENCE · 论证链 | CLAIMS → EVIDENCE |
| 右栏分节 | 方法卡 · PROTOCOL | 方法卡 |
| 右栏分节 | 导师三问 · 组会预案 | 导师三问 |
| 右栏分节 | 点 ＋ 收入术语表 | 删（＋ 按钮自带 title） |
| 右栏按钮 | 让师兄写眉批（逐句吐槽） | 让师兄写眉批 |
| 右栏空态 | 还没有析读。＋ 一整句解释析读会做什么 | 还没有析读。 |
| 角色卡 | AI 按写法推断的角色 · 你可以改判 | 删（下面那个下拉框本身就是这句话） |
| 页边书签 tooltip | ¶3 · 核心主张（点开可改判） | ¶3 · 核心主张 |
| 快捷键卡标题 | 键盘 · 按 ? 收起 | 键盘（卡内最后一行已写明 Esc 收起所有浮层） |
| 设置 · BASE URL | LLM BASE URL（任意 OpenAI 兼容端点） | LLM BASE URL（placeholder 已给出形状） |
| 设置 · 视觉模型 | 视觉模型（框选问答/图表讲解用，留空禁用） | 视觉模型（留空禁用） |
| 设置 · 演示模式 | 演示模式（不调用 API，用假数据走通界面） | 演示模式（不调用 API） |
| 设置 · 图层 | 图层（页边显示） | 图层 |

**两条刻意不删**，并把判据写进 visual.md §5：
1. **隐性交互的操作指引**——"拖入 PDF · 或点击导入"、"在页面上拖一块区域，松手就问"。删了功能就不可发现，这不是说明，是入口。
2. **承诺与反馈**——"设置 · 只存本机"、"API KEY（写入本地 config.yaml，不入库不外传）"、所有 toast。这是用户在意的边界，不是修饰。

判据一句话：**删掉之后，用户会不会因此少知道一个事实或找不到一个功能？**
 
实测（1440×900，真实论文 + 已完成的析读/眉批数据）：骨架/速览/提问/术语四个页签与角色卡、快捷键卡、设置弹窗的可读文本全部重扫一遍，残留 0 处；删除图例下方那行后，图例到 GAP 的间距仍是 14px（`.role-legend` 自带 `margin-bottom:14px`），版面无错位。

### M3.9 · 纯白底 + 护眼底纹 + 拖拽浮层退不出去（用户三条）✅ 已完成（2026-09-11）

**① 拖拽浮层「进得来、出不去」**（用户："这个是什么时候触发的，我不小心触发了，还退不出去"）

浮层是拖 PDF 到窗口上时提示"松手，放到书桌上"。原来只挂了 `dragover` / `dragleave` 两个事件，三处边界全漏：

| 漏的地方 | 后果 |
|---|---|
| `@dragover.prevent` 无条件触发 | 拖**任何**东西都弹——拖选中的文字、拖页面里的图片也算 |
| `@drop.stop` 挂在书桌上 | 把 PDF 拖到论文上松手时，drop 根本冒不到 `.app`，那个负责 `dragOver=false` 的处理器永远不执行 → **浮层赖着不走**。更糟：drop 没被 `preventDefault`，浏览器会直接导航到那个 PDF，把整个应用冲掉 |
| 只在 `.app` 上听 `dragleave` | 松手落在窗口外、或窗口失焦时，事件不落到 `.app`，没人清 |

改法：拖入逻辑收成一个统一入口。只有 `dataTransfer.types` 含 `Files` 才算拖文件；`dragover` 仅在带文件时 `preventDefault`；`dragleave` 靠 `relatedTarget == null` 判断"真的离开窗口"（否则指针在论文上游走会被子元素的 dragleave 疯狂开关）；`dragend`、窗口 `blur`、`Esc` 都补上复位；书桌上的 `@drop.stop` 删掉，让 drop 冒到 `.app` 统一收。

实测六种边界（合成 DragEvent，Chromium）：

| 场景 | 期望 | 实测 |
|---|---|---|
| 拖文件进窗口 | 出现 | ✓ 出现 |
| 拖选中的文字 | 不出现 | ✓ 不出现 |
| 拖文件进来后松手落在窗口外 | 清掉 | ✓ 清掉 |
| 指针移出窗口（relatedTarget 为空） | 清掉 | ✓ 清掉 |
| 指针在论文上游走（子元素 dragleave） | 不被误清 | ✓ 保持 |
| Esc / 窗口失焦 | 清掉 | ✓ 清掉 |

drop 打在论文上：冒到 `.app`、浮层清掉、`defaultPrevented = true`（浏览器不再导航走）——这条同时修掉了一个隐藏的"拖一下整个应用就没了"。

**② 底色改纯白**

用户："整个背景不要现在的纸的颜色，就用纯白"。v3 的亮灰纸（#F4F2ED）连同桌面顶部那层暖色光晕一起去掉，四个 token 全白（桌面/面板/内容），沟槽留 `#F6F6F5` 一丝灰，否则页边批注带和桌面分不开。纯白底上论文页靠 `--shadow-page` 浮起来。略读蒙纱也从写死的暖纸色 `rgba(239,232,216,.9)` 改成 token `--veil`——白底上盖白纱才看得见（现在只有字淡下去，纸色不动，反而比原来那层米色带子干净）。

**③ 护眼底纹（设置里切，立刻生效，只存本机）**

用户："设置里面加一个护眼模式（你查一下护眼模式的颜色怎么调）"。查证结果：豆沙绿 `#C7EDCC`（199,237,204）是流传最广的经典值，另有浅青绿 `#CCE8E8`、米黄 `#F5F5DC`/`#FFF8DC`；这三个都源自 Office 2003 浅绿底（RGB 192,232,207）一脉的历史惯性，**ISO/IEC 9241、WHO、AAO 均未认证任何"护眼标准 RGB"**——有资料给出蓝光占比（豆沙绿 26%、纯白 33%）和 NASA-TLX 主观疲劳下降 31% 之类的数据，但都缺大样本临床验证；真正管用的是亮度、色温与看远处休息。所以做成四个可选项（纯白 / 豆沙绿 / 浅青绿 / 米黄），不是"药方"。

实现要点：**纸面上盖一层 `mix-blend-mode: multiply` 的色膜**，不是整屏蒙版也不是 `filter`。multiply 只乘"底"——白纸乘成护眼色，黑字乘完还是黑字。实测纸面 `#CDEFD1` 时正文仍有 **16.9:1** 对比度（纯白纸 21:1），一个字的清晰度都没让出去。桌面/面板换同色系浅底（桌面比纸面更浅，纸才浮得起来），`.page` 加 `isolation: isolate` 把洗色圈在纸内。

对比度全部重算（`_qa/care_contrast.py`，三档底纹 × 三级墨 × 洗色后的纸面）：最差 4.56:1，全过 AA。**顺带查出一个老问题**：三级墨 ink-3 原来是 `#8a867c`，在纯白和护眼底上只有 3.2–3.6，小字本来就不达标（不是这轮引入的），一并压到 `#6b675e`。

顺手修的：设置弹窗里"骨架标签/眉批"两个复选框的字被 `.f-row label{display:block}` 顶成竖排。

### M3.10 · 18 条反馈：阅读器基本盘、精确划线、文案再清（用户逐条列）✅ 已完成（2026-09-11）

用户列了 18 条并要求"举一反三修同类"。按"一处根因、一批同类"归并处理：

**A. 三类真 bug（不是审美，是坏了）**

1. **放大后论文左边滚不回来**（第 1 条"非全屏时展示逻辑不好"，实测放大到 205% 时论文左边缘在 −161px，横向可滚的 207px 全在右边）。根因是 flex 的 `align-items: center` 在内容比容器宽时会把左半边推到滚动区之外。改成 `width: max-content; min-width: 100%; margin: 0 auto`。
2. **窄窗排布**（同第 1 条）：1100px 窗口里，固定 336px 右栏 + 46px 左条把论文压到 452px 宽，两栏小字根本读不了。右栏在 <1180px 改成**浮层抽屉**（不占版面），跨门槛自动收起一次；<1000px 让出顶栏标题。实测同一窗口论文从 452px → 788px。
3. **`desk-inner` 宽度的正反馈**：`max-content` 之后 `clientWidth` 是内容宽，适宽比例成了"纸越大容器越宽纸更大"，一路顶到 300%。适宽/适页改量 `.desk`（真滚动容器）。

**B. 划线/跳转的精度（第 12 条，本轮最大的一块）**

- **后端补行级坐标**：段落原本只有段落框，拿它画高亮就是整段涂。`pdfparse` 现在每个段落带 `lines[{text,bbox}]`（**分组逻辑一字未改，所以 idx 不变，已有批注不会错位**）；旧库在 `GET /paragraphs` 时惰性重解析一次补齐。
- **引文 → 字符 → 逐行矩形**（新模块 `frontend/src/find.js`）：把引文和后端行文本都归一化（只留字母数字汉字、小写、连字符与空格全丢，于是 "pro-jector" 与 "projector" 同形），对回字符区间，再按文本节点分组取矩形——pdf.js 的文本层一行一个 span，所以这正好等于"划了哪几行、每行划多长"。实测 20 条批注 → 38 个划线块，1 行 4 条 / 2 行 14 条 / 3 行 2 条，最窄一块 31px（行尾的 "0.1 eV"）。
- **踩到的两个坑**：① `range.getClientRects()` 跨文本节点时会多吐几像素宽的碎片（实测 147/3/3/161），改用"按节点分组、每节点取最宽一条"；② 模型抄引文**不是逐字**的（实测有一条把原文的 stochastic 写成 stoichiometry），逐字匹配会让整条划线消失 → 补"按词退到最长能对上的前缀"，划线只盖真实存在的字。
- **框选钉子与引文钉子分家**：原来两者都靠 `rect` 且都用一份不精确的 `search_for(截断引文)` 结果（只取了第一行）。现在框选钉子 `kind='region'`（矩形就是唯一真相），引文钉子一律回原文重新对；`_resolve_rects` 降级为纯退路。
- **跳转落到行**：点眉批卡上的引文直接跳到纸上那一句；右栏眉批速览也走同一条路。实测落点误差 0px。

**C. 阅读器基本盘（第 9 条，先查了 Zotero 等阅读器该有什么）**

查证结论：Zotero 7 的阅读器基本盘是缩略图 / 批注列表 / 大纲 / 适应宽度与页面 / 页码 / 查找 / 高亮下划线 / 键盘。**只做科研真用得上的**：适应宽度 · 适应页面 · 实际大小 · 百分比微调 · 页码直达 · 上下页（`PageUp`/`PageDown`）· **论文内查找（Ctrl+F，逐处高亮 + 逐个跳转）** · 缩放与阅读位置记忆。**明确不做**：旋转、缩略图侧栏、书籍双页、多语言界面——论文阅读用不到，纯涨界面。

**D. 文案与控件（第 2/3/4/5/7/11/13/15/17/18 条）**

| 位置 | 处理 |
|---|---|
| 框选提示 | 只剩「框选 · 退出」，删掉"在页面上拖一块区域，松手就问"与 kbd 提示 |
| 整本翻译 | 译完就**不再显示**（留一个永远点不动的按钮只会让人猜它还能干什么）；含义移到 hover title |
| 设置 | 加右上角 ×；标题去"只存本机"；API KEY 去"不入库不外传"；护眼底纹去"立刻生效，只存本机" |
| v0.1 / 段数 | 删。总量并进右栏读数：`读至 ¶13 / 23` |
| GAP / CLAIMS → EVIDENCE | 改中文：`研究缺口` / `论点与证据 · 5 条`（用户问"这是想表达有多少个论点及其证据吗"——是） |
| SEED 标签 | 删（术语来源里的内部词，用户看不懂） |
| 速览黄条 | 删（一眼卡标题那层 highlighter 渐变） |
| 术语添加行 | 溢出根因是 flex 子项 `min-width:auto`，输入框按默认宽度撑破整行；`min-width:0` 修掉 |
| 图层一行 | 两个复选框 + 演示模式并成一行 19px（原来各占一整行） |

**E. 交互（第 6/10/14/16 条）**

- **所有浮窗可拖**（新指令 `src/drag.js`，见 visual.md §7）：设置弹窗、快捷键卡、角色卡、划词/框选气泡、阅读器控件条。位移写 `transform` 不动 `left/top`；拖过就吞掉那次 click（否则拖完弹窗被"点外部"关掉）；角色卡被拖动后不再粘回书签。
- **段落角色点开摊全部位置**（第 10 条：一个角色往往十几段，只跳第一段等于把其余位置藏了）：点一类角色，在图例下方摊开它的全部 ¶ 号，逐个点着去。实测"关键证据"11 段一键展开。
- **删论证漫游 ▶**（第 14 条：黄色高亮有延迟、位置也不对）：连同 `tourClaim`/`tourStop`/`.ci-tour` 一起删。跳转的琥珀色闪烁保留，但改成落在**引文的精确矩形**上，且只在单次跳转出现（不再有 2.6s 一跳的循环）。
- **图表灯箱左右翻图**（第 16 条）：‹ › + ←/→，带 `n / N · 第 x 页` 计数。

**F. 两个只有实测才发现的坑**

- **`focus()` 会把书桌滚走**：查找框挂在书桌内容末尾，浏览器为把焦点滚进视野会把整个书桌滚到底，把刚跳好的位置冲掉（实测偏移 2400px）。改 `focus({ preventScroll: true })` 后 7 处命中逐个落位误差 0px。
- **远距离平滑滚动**：跨几页的 `behavior:'smooth'` 要滚一两秒，翻页变成等动画。改成"近处滑、远处切"（距离 >1.5 视口高就即时）。

### M3.11 · 7 条反馈：主色换靛青、标记重做、缩放分清界面与论文 ✅ 已完成（2026-09-11）

**① 主色：琥珀 → 靛青**（用户："不要以现在的屎黄色作为主色调，高级一点，你去网上搜搜"）

先查了再定：设计圈讲"高级感"反复出现的就是**克制**——低饱和、中性色主导、彩色只留一个（60-30-10），深蓝/藏青配中性灰是最稳的一组；再往里翻，中国色的靛青/藏青（`#177cb0` / `#2e4e7e` 一脉）比纯蓝更接近墨色，和纸质阅读器的调性对得上。定为 **`#1D4E5F`**（深档 `#123A47`）。

顺手查出一条硬伤：**白字压在原来的琥珀上是 2.86:1**——主按钮一直没达到 WCAG AA。靛青白字 9.10、白纸上的靛青 9.10、三档护眼纸上 7.22–8.31，两头都富余（`tools/contrast_check.py` 里新增这一段，换色先看它）。

角色色标同步：核心主张那一档从琥珀改成靛青（"全文的芯"本来就该是强调色），眉批的"点睛之笔"同理；其余三档灰不动，注意力阶梯不变。

**② 品牌标记重做**（用户："logo 还是很丑，去网上学一下 logo 设计，可以推翻现在的"）

查到三条硬原则：**几何骨架先行**（先定圆形/方形/三角形这类基本形，再谈细节）、**别画字面图标**（面包店不必画面包）、**缩到 16px 还能认**。对照旧标记——一只带裂缝的蛋，正是典型的字面图标，而且真被读成过"水滴"。

新标记是一枚**印章**：椭圆环 + 环内几行字（末行短一截）。环是纸的边界，字是内容；椭圆环又刚好是蛋的轮廓，名字里的 egg 靠形状带出来。几何上上下两个半椭圆在最宽处相切（上 ry 34 / 下 ry 42，钝端朝上），环内字条按椭圆在该高度的内接宽度递减。小尺寸另备一稿（环加粗、字条三条），`compact` 开关切换。实测 128/80/48/32/24/16px 都立得住（`docs/screenshots/mark.png`）。

中间试过并否掉四版：掀盖的蛋（像"戴帽子的蛋"）、实心蛋横切一刀（像贝雷帽）、文字条堆成蛋形（像汉堡菜单）、实心蛋挖个洞（像贝加圈）——都做了尺寸对照才看出来不行。

**③ 缩放：把"缩论文"和"缩界面"分清**（用户："论文的缩放还是不好用，要区分浏览器的缩放和论文的缩放"）

- **`Ctrl+滚轮` = 只缩论文**（光标在纸上时，`preventDefault` 掉浏览器的缩放）：这是读 PDF 的人肌肉记忆里的动作，也正好是两者该分开的地方。`Ctrl+±` 仍然是浏览器缩放，去缩整个界面。
- **锚点落在光标底下那一行**：缩放前记下光标处的「第几页 + 页内比例」，缩完按新高度落回同一比例。实测放大两次、缩回两次，页内比例 0.104 → 0.104，**漂移 0**。
- **逐档查了溢出**：把视口按 1440 / 1152 / 960 / 822 / 720（≈ 浏览器缩放 100 / 125 / 150 / 175 / 200%）跑了一遍——顶栏不超宽，书桌横向溢出 0，论文不出界，右栏在 <1180 自动变浮层，快捷键卡/设置弹窗/文库在最窄档也都在屏幕内。论文缩放不动顶栏、不动右栏（实测宽度 1707 / 336 / 56 前后一致）。

**④ 其余四条**

| 条目 | 处理 |
|---|---|
| 框选气泡左下角的"关闭" | 删（右上角已经有 ×，Esc 和点外也能关；底部只留"钉在页边"这个真动作） |
| 框选问答的回答排版 | 抽出共用的 `MdLite`（`src/md.js` + `components/MdLite.vue`）：标题 / 列表 / **加粗** / `行内代码` / 可点 ¶ 引用；问答面板与框选问答从此同一套排版。回答区加了 268px 上限，长回答在框内滚，气泡不撑出屏幕 |
| 论点项的黄色渐变高亮 | 整套删（`:class="{ now }"`、`currentClaim`、`.claim-item.now` 的渐变） |
| 图表灯箱"第 x 页" | 删（左上角已经有 `n / N`） |

**⑤ 顺手修的一处通用问题**

视觉问答失败时用户只看到"500"——因为后端让异常裸奔，FastAPI 返回的是纯文本 `Internal Server Error`，前端拿不到 `detail` 只能显示状态码。加了一个全局异常处理器，把模型服务最常见的几种失败翻成人话（429 →「模型服务限流了，等一会儿再试」、401 →「API KEY 无效」、超时 / 余额 / 模型名不对各有对应），前端再兜一层 `status + statusText`。实测（当时正好被上游限流）：气泡里显示的是「模型服务限流了（429），等一会儿再试」，而不是"500"。

### M3.12 · 提问做成对话、文库分类、栏宽可拖、页边按需给宽 ✅ 已完成（2026-09-11）

**① 提问模块重做成 DeepSeek / 豆包网页版的样子**（用户点名"这是重点"）

先查了那几个产品的交互清单，好用的原因是四条（详见 visual.md §9）：流式逐字 + 中途可停、一摊对话一个上下文、每条消息一排动作、输入框长高且 Enter 发送 / Shift+Enter 换行。据此重写右栏「提问」为 `AskPanel.vue`：

- **后端**：新增 `conversations` 表（一篇论文多摊会话），`/ask` 改成 **SSE 流式**（`delta` / `done` / `error` 三种事件），`llm.chat_stream()` 走 `httpx.stream` 解析 `data:` 帧；`qa_messages` 加 `conv_id`，旧的孤儿问答自动收编进一摊「此前的提问」。`/regenerate` 撤掉最后一问一答再重问；`/qa-save` 收中途停止的半截答案。
- **前端**：用户消息靠右小气泡、助手通栏；流式尾巴一个步进光标；悬停出「复制 / 重新生成 / 删除」；「依据 ¶n」与正文里的 ¶ 号都可点回原文；会话下拉 + ＋/✎/🗑；空态给推荐问题；输入框 `isComposing` 时 Enter 不发送（中文输入法选字不能误发）。
- **实测**：发→流式→中途停止→半截落库；重新生成撤掉并重问；删除删的是真行；会话切换与计数正确。

**② 一个只有实测才发现的 bug：删除删不掉**

流式新发的那两条消息在本地是没有 id 的（服务端才知道），于是"删除这条"只删掉了屏幕上的，刷新又回来了。修法：把落库 id 顺着 `done`/`error` 事件回传（`user_id` / `assistant_id`），中途停止的那条由 `qa-save` 的返回值补上。这个 bug 值得记一条：**"界面删掉了"和"数据删掉了"是两件事，凡是"删除"都要问一遍删的是谁**。

**③ 文库：分类 + 搜索 + 排序**（用户："可以有分类（zotero 的功能）等功能"）

抄 Zotero 左栏的两个要点：**一篇文献可同时属于多个分类**（像标签不像文件夹）、**分类不是唯一入口**（随手能搜、能按最近读排序）。层级树、保存的检索式、自动标签一律不做（几十篇的体量下是负担，见 ux.md §7）。实现：`collections` + `paper_collections` 两张表，`/api/collections` 一次返回分类与归属映射；条目右上角数字点开勾选，也可以**直接把条目拖到左侧分类上**；导入时若正停在某个分类，新文献自动归入该类（Zotero 的"导入到分类"）。

**④ 删除：本地要真删干净**

`purge_paper()` 把段落/骨架/眉批/问答会话/分类归属一张一张点名列，再删原 PDF、双语版、译文版（按 pid 扫 `data/translated` 防漏），前端顺手清掉 localStorage 里的阅读位置。实测：删两篇测试文献后，七张表的孤儿行数全为 0；后台析读跑完时论文已被删除的情况也补了守卫（否则会往空论文上灌数据）。

**⑤ 栏宽可拖（右栏 + 文库）**

按住面板内侧边缘拖动改宽度，双击复位，方向键 ±28px 也能调；宽度进 localStorage。**拖的过程中不重新给论文定标**——每帧重算 fitScale 等于每帧重画九页画布，所以拖动期间只动栏边，松手才重排一次并按锚点落回原行。实测：336 → 516px 拖动跟手，松手后论文 156% → 126% 且不产生横向溢出，双击回到 336。

**⑥ 页边与书签：按需给宽、不许越界**

- 页边宽度改成"有话才说"：有批注 184px、只有书签 24px、图层全关 0px。实测同一篇论文 156% → 183% → 188%，且都不产生横向滚动。
- 角色书签原来一路往下让，密集页能伸到下一页的页边上；现在让完**量一次越界**，越了就整体压缩间距（宁可贴住也不翻页）。实测 23 个书签全部落在本页内。
- 图层开关会改页边宽度，所以切图层后要重新定标并落回原处（原来不重排，纸宽是不会变的，现在会变）。

**⑦ 速览按钮改「获取」**：方法卡与导师三问未生成时按钮就叫「获取」（`title` 里保留一句它到底做什么）。顺手去掉导师三问在析读完成后的**自动预生成**——那是要花 token 的调用，没点就不该发生。

**⑧ 借"动作"的三处动效 + 一枚可选皮肤**（用户："借动作做几个动效；做一枚蛋仔化的可选皮肤"）

- 空态拖入时印记**原地弹跳**（蓄力—起跳—落地压扁，1.05s 循环）；析读 / 整本翻译完成时 wordmark 标记**滚半圈**（0.68s，`transform-origin: 50% 85%` 所以是底边着地滚）；略读蒙纱按段落顺序**从左往右扫**出来（stagger 12ms + clip-path 380ms）。
- ~~「蛋仔」皮肤~~：底/墨/主色/圆角四样换掉 + 标记变一颗圆滚的蛋（暖底上最差 5.19:1、白字压暖橙 5.82:1，都过 AA）。**后来整块删除**，见 M3.14。三处借"动作"的动效保留。

**⑨ 边界情况：这一轮最像"扫雷"的部分**

| 情况 | 处理 |
|---|---|
| 扫描件（没有文字层） | 导入不再崩（`statistics.median([])`：整页只有一行文字时行距中位数没有样本，原来会让**整篇导入失败**）；`_require_paras` 让析读/一眼卡/推荐问题/导师三问/方法卡/提问一律直接给出人话（能读原文、图表能框选问 AI），一眼卡也不再永远转圈 |
| 不是 PDF / 文件不完整 | 400 + 人话（"%PDF" 头 + 最小体积两道闸，阈值取 256B——太小会把合法的空白页 PDF 也拒掉） |
| 解析失败 | 删掉刚落盘的半成品文件，不留"点开是空书架"的论文 |
| 删掉当前打开的那篇 | 书桌回到空态、右栏**整个隐藏**（一个只有页签的空栏目会让人以为它坏了，且里面的问答会对着不存在的 paper_id 发请求） |
| 后台析读跑完时论文已被删 | `set_analysis` / `set_marginalia` 先查论文还在不在 |
| 窗口窄到 1000px | 右栏变浮层、宽度取 `min(栏宽, 92vw)`、顶栏让出论文标题；全档无横向溢出，收起右栏不再改变纸宽（浮层不占版面） |

### M3.13 · 读进去的那几下、上下文不丢、文库去噪 ✅ 已完成（2026-09-11）

**① 划词气泡：点外面就该关**（用户反馈）

原来只有"点非纸面区域"才关（`onDocDown` 把 `.textLayer` 排除在外），于是在纸上点哪儿都不关，只能去够那个小叉。当时的顾虑是"别把正在划的词弄丢"——其实点下去会清掉选区、`mouseup` 又会按新选区重开气泡，什么都不会丢。去掉那个例外后实测：点纸面关、点右栏关、点气泡内部不关（三种都验过）。

**② 加载文案统一成一家人的说法**：`正在破壳`（打开 PDF）· `正在拆骨架`（析读）· `正在写一眼卡`（速览）· `正在看图`（视觉问答）。都是动作 + 一个名词，不再解释自己要干什么。

**③ 让科研人真的能读进去**（用户："互动，比如打开文献能看到一些文案、互动之类的，以及看文献中途，能让人真的轻松看进去"）

这里很容易做成"又加了一堆解释文案"，所以刻意只加**动作**，不加说明：

- ~~读段提示~~：视口中心那一段一换，纸下缘报一句 `¶8 · 标准流程` + 「译这段 / 问这段」。**做出来之后被判定为多余，已彻底删除**（浮层挡在纸上、每换一段就冒一次，读者要的是不被打断）；代码、样式、文档一并撤掉。留下的替代是键盘 `t` / `/`——同一个动作，不用浮层。
- **阅读进度**：书桌右缘一根 3px 发丝，读到哪长到哪，不写字、不报数。
- **打开就有回应**：有眉批的论文一打开，划线和批注卡自己"画"进来一次（300ms），像有人刚在纸上划过。
- 顺手修一个"闪别人家内容"的 bug：换篇时上一章的骨架与眉批会短暂画在新论文的页面上（`openPaper` 只清了 summary 和 qa）。现在换篇先清 `paras / analysis / marginalia / readingPara` 再装新的。

**④ 提问的上下文：全量、同步删除、超限压缩**（用户："一个会话之前的提问和回答也要进上下文；删除了某个提问也不要加入；超限要压缩但不丢关键信息"）

原来只带 `history[-8:]`——更早的轮次**静默消失**。现在：

- **全量进上下文**（按会话，跨会话隔离）；被删掉的消息自然不在里面（上下文就是从库里读的），删到"已被折进摘要的那条"时摘要一并作废重建——**"删了就不在上下文里"对摘要同样成立**。
- **超限压缩**：12 条 / 6000 字以上开始折，保留最近 8 条原文（够接住"刚才那个""再详细点"这类指代），更早的交给模型压成摘要（明确要求保留：已确认的结论与数字、依据段号、定下的术语译法、用户反复关心的点、未解决的问题），存回会话并只折一次（`summary_upto` 记账，不会重复折）。
- **压缩失败（限流/超时）时不许静默丢**：把老消息截短了照样带上——粗糙好过失忆。这一条是写代码时自己发现的设计漏洞，后来专门加了测试（摘要失败 → 20 条全都还在，只是变短了）。
- 界面上给一行「更早的对话已存为摘要」，悬停能看摘要全文。

**⑤ 文库列表去噪**（用户逐条点名）

收起按钮从 `esc` 字样的按钮改成一个 `‹`；条目左侧的蛋形 logo 删掉，只留 `＋`（归入分类）；`9p` 与三根状态横条删掉，空出来的位置写**第一作者**（`pdfparse.extract_authors`：只认标题正下方那一两行里的第一个名字，剥掉上标/邮箱/日期/机构；认不出来就返回空、界面上不显示——绝不拿机构名或日期凑数）。实测：真论文认出 `Shuxiang Zhou`，扫描件/单行页/空白页都返回空。归入分类的反馈改由 toast 说（"已归入「水分解」"），行上不再挂看不懂的数字。

**⑥ 提问模块的文案**：会话相关按钮/弹窗里的"一摊"全部删掉，改成 `切换会话 / 新建会话 / 重命名 / 删除会话`；输入框 placeholder 去掉括号里的按键说明；空态那句解释 ¶ 号的话也删了（悬停就能看出可点）。举一反三扫了一遍全站的括号注释：只留下**真有信息**的（`视觉模型（留空禁用）`、`整本翻译服务（pdf2zh）`、`openai（OpenAI 兼容）`），键盘提示一律保留（那是唯一能看到快捷键的地方），解释性的全删。

### M3.14 · 删掉蛋仔皮肤，只留原始一套 ✅ 已完成（2026-09-12）

用户："完全删除皮肤这个板块，删除蛋仔皮肤，只保留原始（设置里也要删掉选皮肤这一栏）"。

一并删掉的：

| 位置 | 删除内容 |
|---|---|
| `styles.css` | `html[data-skin='egg']` 整段（纸/墨/发丝线/主色/veil/code-bg + 八处圆角覆盖）、`.modal .skin-chip` 与 `.skin-dot` 预览点 |
| `EggMark.vue` | 蛋形那一支（`eggy` 分支、`EGG_BARS`、蛋壳路径与高光），只留印章；顺带去掉了不再需要的 `store` 导入 |
| `SettingsModal.vue` | 「皮肤」整栏（`SKINS` / `pickSkin` / 两个预览块） |
| `store.js` | `viewer.skin` 字段与那段写 `<html data-skin>` 的 watch；只留一行清掉旧版本可能留在 DOM 上的属性 |
| 文档 | visual.md 的「皮肤（可选）」整节与截图、§3 那段皮肤说明；ux.md 交互清单的皮肤行；README 的功能项、截图行与标记段落 |
| 截图 | 删 `docs/screenshots/skin-egg.png` |

为什么删而不是留着：**一套设计系统只该有一副面孔**。多一套皮肤不是多几行 CSS，而是多一整套需要各自验算的对比度、圆角与色标关系（当时就已经出现"界面是橙的、核心主张色块还是靛青"的两色打架）。玩心留在**动作**上——空态拖入原地弹跳、析读完成滚半圈、略读蒙纱横扫，这些不花一分对比度的账。

验证：构建通过；源码与 dist 里 `data-skin` / `皮肤` / `蛋仔` 残留均为 0（只剩 store.js 里那行清属性的注释）。

### M3.15 · 划词翻译提速：非流式改流式 + 关掉推理模型的"思考" ✅ 已完成（2026-09-12）

用户："正文勾选的内容直接点击翻译出结果有点慢"。实测点击翻译到出结果要 **10–20 秒**。

**排查过程**（每一层都量了一遍，别猜）：

1. 先怀疑非流式：同一请求流式首字 0.8s、非流式要等全尾 10–20s → 把 `translate-selection` / `translate-para` 改成 SSE 流式（`_translate_sse`，delta/done/error 三种事件；术语命中随 done 回）。改完端到端首字仍是 **10.6s** ——不是（只是）非流式的锅。
2. 再怀疑服务端缓冲：最小 uvicorn 应用逐帧到（0.39/0.69/0.99…）、`iterate_in_threadpool` 逐帧到、CORS 是纯 ASGI 不缓冲、`glossary_hit` 与 `config.load()` 都是毫秒级——排除。
3. 在生成器内部打点：上游响应头 2.4s，**第一个有内容的帧 6.5s**。回头重测直连，发现之前量的"首帧 0.8s"量到的是**第一行**（可能是空行），不是第一块内容——真正的首内容帧也要 6.7–9.6s。
4. 看首内容帧之前收到了什么：全是 `reasoning_content` 帧——**glm-5.3-flash 是推理模型，输出正文前先思考 5–10 秒**。这才是"慢"的真正来源。非流式更糟：思考 + 正文全部等完。

**修法（两层）**：

- **翻译走流式**：译文逐字出现（带光标），术语命中随 `done` 事件回传；换选区/关气泡会掐掉进行中的流，不再往已关的气泡里写字；段译同样流式，钉页边时攒成品一次性钉（页边卡不逐字跳）。
- **翻译关掉思考**：`chat_stream(no_think=True)` 给推理模型带 `thinking: {"type":"disabled"}`（智谱系参数；不认识的 OpenAI 兼容端点会忽略这个字段，坏了也不伤）。翻译要的是准确与快，要不了那个推理深度。

**实测**：端到端首字 **10.6s → 1.6s**；浏览器里点「翻译」到译文出现 **2.95s**（含 React/Vue 渲染与上游波动），完整译文 3 秒内。译文质量抽查无回退（术语与数字保留原样）。

一句话沉淀：**"慢"不一定是代码慢，先问模型在干什么**——推理模型的"思考"藏在上游响应头之后，量"首行"量不出来，要量"首块有内容的帧"。

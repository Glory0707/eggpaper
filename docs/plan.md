# eggpaper 实施计划

> 配合 design.md v0.2。每个任务带验收标准；每个里程碑结束做一次功能走查。

---

## M2 · 翻译三模式 + 界面减法 + 一致性补漏（下一轮实施）

### T1 翻译三模式

| # | 任务 | 验收 |
|---|---|---|
| T1.1 | 后端 variant=mono（mono.pdf 已在 data/translated，补路由与状态判断） | 「译文」模式加载 mono.pdf；未生成时按钮置灰 |
| T1.2 | 顶栏 segmented 扩为 原文 / 译文 / 双语 | 三态切换正确，切换后保留当前缩放与页码附近位置 |
| T1.3 | 双语对开视图：左原文页 + 右译文页成对滚动（dual 偶数页配对），双文档渲染 | 9 页论文渲染 9 个 spread；左右页码对应；无渲染饿死 |
| T1.4 | 双语子选项「对开 / 交替」，默认对开 | 两模式切换即生效；选择记忆到 localStorage |
| T1.5 | 角色/眉批图层在三模式下行为定义：原文与对开的原文页显示批注；译文页与整版译文不显示 | 各模式截图确认 |

### T2 界面减法（逐项过，删完复查）

| # | 删除/修改项 | 验收 |
|---|---|---|
| T2.1 | 删页面底部 colophon 免责行；"推断"提示移入角色卡内部一行小字 | 页面无固定文案残留 |
| T2.2 | 删左栏底部「文献·术语·批注 全部留在本机」 | 同上（该信息只留在设置弹窗） |
| T2.3 | 顶栏 VOL.0.1 · 阅读过默 → 仅 v0.1 | 视觉噪点减少 |
| T2.4 | 文档元信息去掉文件名（标题已展示） | 「9 页 · 23 段」足够 |
| T2.5 | 角色图例只显示计数>0 的角色 | 图例更短 |
| T2.6 | 全 UI 文案通读一遍：重复出现的信息只保留一处 | 走查通过 |

### T3 术语一致性补漏

| # | 任务 | 验收 |
|---|---|---|
| T3.1 | 问答/总结 system prompt 注入命中术语表 | 问"这篇的 trade-off 结论"回答使用锁定译法 |
| T3.2 | 术语表导出 CSV → pdf2zh `--glossary-files` | 整本双语中该术语译法与术语表一致 |

### T4 功能走查（每功能实测顺手度）

走查协议：每项功能按脚本操作，记录 完成秒数 / 卡点 / 情绪；卡点修复或砍功能，不修说明理由。

| 功能 | 任务脚本 | 通过线 |
|---|---|---|
| 导入 | 从 Zotero 拖一篇 PDF 进窗口 → 自动解析 | ≤20s 看到可读页面 |
| 析读 | 点析读 → 等待 → 骨架出现 | 等待有进度反馈，结果一眼能懂 |
| 精读 | 开略读扫核心段 → 关略读 → hover 任一标签 → 改判一个角色 | 改判立即生效并持久 |
| 眉批 | 生成眉批 → 点一条跳原文 → 看高亮定位 | 定位准确率抽查 ≥90% |
| 翻译 | 三模式各切一遍；划词翻译一个术语；收进术语表 | 全程无说明文字也能完成 |
| 问答 | 问固定 3 问 → 点引用跳转 | 引用跳转准确 |
| 删除 | 删一篇 → 文库与视图正确清空 | 无残留 |

**M2 出口验收**：三模式可用 + UI 无冗余文案 + 走查清单全过 + 截图存档 `_qa/shots/`。

---

## M2.5 · 阅读工效（交互设计落地，见 ux.md）

| # | 任务 | 验收 |
|---|---|---|
| T5.1 | 位置记忆：重开论文恢复滚动位置+模式+图层 | 关掉重开回到原位 |
| T5.2 | 跳转栈：所有跳转入栈，Alt+← 返回 | 引用跳转后能一键回原位 |
| T5.3 | 键盘流：j/k/t/s/f/1/2/3///x/g l/Alt+←/?,? 卡可自发现 | 全程不碰鼠标完成一次精读循环 |
| T5.4 | 查译钉页边：划词气泡加「钉在页边」，查译卡与眉批同列、标"你" | 关气泡后查译仍在，导出可见 |
| T5.5 | 段落把手：hover 段落页边浮出「译此段/问这段」，段译钉页边 | 不碰右栏完成段级翻译与提问 |
| T5.6 | 左栏收纳 44px 图标条（浮出面板）+ 右栏可折叠 + 双语对开自动收右栏/隐藏页边 | 阅读态正文宽度显著增加；三态姿势自动切换 |
| T5.7 | 角色改判"已改"角标 + 回到推断 | 撤销路径可用 |

**M2.5 出口验收**：走查协议重跑一遍（导入/析读/精读/眉批/翻译/问答/删除），全部通过线不变；另加"全程键盘精读循环"通过。

---

## M2.6 · 视觉与动效打磨（按 visual.md token 全量落地）

| # | 任务 | 验收 |
|---|---|---|
| T6.1 | 字体四声系统落地：serif 内容 / sans 控制 / mono 标签 / kai 人声，字号阶梯收敛到 7 档 | 一屏内家族 ≤3、字号 ≤4 |
| T6.2 | 配色 token 化：纸面四层亮度分层、墨色三级、阴影=悬浮纪律（静态布局去阴影） | 目检 + 截图对比 |
| T6.3 | 动效 token 化：三缓动三时长；气泡/折叠/模式切换/位置恢复按清单执行 | 所有过渡 ≤320ms |
| T6.4 | 点睛三件：略读 stagger 波浪、眉批引文描画+页边引线、析读笔画进度线 | 录屏 60fps 目检 |
| T6.5 | `prefers-reduced-motion` 全局支持 | 系统开启后零动画 |
| T6.6 | 微巧思：拖入蛋壳裂纹、析读完成蛋黄一颤（各仅一个触发态） | 不影响主流程 |

**M2.6 出口验收**：visual.md §5 五条全过 + 走查协议重跑。

---

## M3 · 沉淀与效率

1. 方法卡片（protocol 卡）：验收=一篇实验论文抽出可复现的材料/参数/步骤三段
2. 本文缩写表：骨架时顺带抽取；进术语 tab；可一键入术语表
3. 图表速览画廊：Figure 连图注；验收=图页论文不用读正文即可讲出主线
4. Markdown 导出：骨架+眉批+查译+一眼卡 → 单文件 .md；验收=导入 Obsidian 不乱
5. 术语导出 CSV/Anki
6. 骨架 mini-map：滚动跟随高亮当前节，"读到哪了"可见（scroll-spy）
7. 图表放大镜：hover 图区放大 + 「问这张图」（与图表速览合流，依赖图形区域检测）

## M4 · 进阶（按需启动）

框选区域视觉问答（需配置视觉模型）· 数据对比表 · 批量速览 · 审稿模式 · Zotero 深度集成 · Tauri 打包 · OCR

---

## 技术债与备忘

- LLM 输出归一化已做（键/枚举/范围校验），新功能沿用，禁止静默吞错
- 推理型模型（glm-5.3-flash）max_tokens 要留思考额度：骨架 16k、眉批 12k，新调用照此量级
- pdf2zh：google 在国内网络挂起（无快速失败），默认服务已是 bing；后续可给 subprocess 加首输出超时探测
- 骨架/眉批 prompt 的角色体系按化学定制；扩展学科时做成可配置模板而非硬编码
- 解析器对单栏/其他排版未系统验证，遇到再说（修复优先于预防）
